# Changelog

## 2.4.4

- 数据恢复改用 AstrBot 每次安装、更新和热重载都会调用的 `initialize()`，修复热更新后内存战绩为空的问题。
- 插件完成数据恢复前禁止写入状态文件和 KV，避免未初始化实例在停用或再次更新时用空数据覆盖历史记录。
- 若 2.4.2/2.4.3 已产生空主文件，会自动尝试从非空的 `state.json.bak`、当前 KV 或旧数据源恢复战绩。

## 2.4.3

- 井字棋固定为 X 方视角，不再随轮次上下翻转，确保 1～9 格位始终一致。
- 规则自然终局时，将最终棋盘与结算卡合为一张纯图片发送，避免部分平台漏掉连续发送的第二张图片。

## 2.4.2

- 将战绩和未结束棋局的主存储迁移到 `data/plugin_data/astrbot_plugin_boardgames/state.json`，KV 改为兼容镜像。
- 状态文件使用临时文件原子替换，并保留 `state.json.bak`；主文件损坏时自动回退，加载失败时禁止覆盖原数据。
- 首次启动自动尝试迁移当前 `astrbot_plugin_boardgames` KV、旧 `astrbot_plugin_chess` KV，以及仍然存在的原版 `chess_stats.json`，且用迁移标记避免重复累加。

## 2.4.1

- 在 `metadata.yaml` 补充 GitHub `repo`，修复 WebUI 无法确定插件更新源的问题。
- 补充卸载重装时保留 AstrBot 插件 KV 数据的说明；若要保留战绩，不要勾选“同时删除插件持久化数据”。

## 2.4.0

- 等待加入的空房现在允许群内任何成员使用 `/流局` 清理。
- 普通对局改为任一选手选边，对手自动分配相反阵营；选边后发送独立对阵卡、棋盘和本局计时说明。
- 新增头像对阵卡与终局结果卡，展示棋种、规则、用时、等级分、总战绩、分棋种战绩、双方交手记录及等级分变化；头像不可用时自动使用昵称首字占位。
- `/对局记录` 支持 `@玩家`、棋种筛选及二者组合，并增加总胜和负、胜率、分棋种汇总和最近十局。
- 围棋默认用时调整为标准业余赛制 `60|3x30`。
- 默认提醒改为全程每分钟、3 分钟内每 30 秒、2 分钟内每 15 秒、1 分钟内每 10 秒、30 秒内每 5 秒。
- 新增六个棋种各自的帮助指令，并重写 `/下棋帮助` 与 `/棋类帮助`。

## 2.3.0

- 所有棋盘改为当前行棋方视角，并在上、下、左、右四边完整显示坐标。
- 普通对局在第二位玩家加入后强制双方选择不同阵营，选边完成才正式开钟；Swap2 保留自身的官方选色流程。
- 新增比赛棋钟、图内双边剩余用时、超时自动判负和分级提醒；支持加秒、包干及围棋多次读秒，并提供开局覆盖和 WebUI 全局自定义。
- 新增 Elo 等级分、最近对局记录和分棋种排行榜；既有胜和负统计继续保留。
- 围棋终局文案明确区分规则面积数子和 AI 估算，并增加 `/围棋计分` 说明。
- 新增棋钟、强制选边和相关交互回归测试。

## 2.2.0

- 插件标识和发布仓库统一更名为 `astrbot_plugin_boardgames`。
- 这是新的插件 ID；尚未部署时可直接使用，新旧 ID 不会混淆。

## 2.1.0

- 五子棋新增自由规则、标准五连和连珠禁手，可在开局时自行选择。
- 新增完整 Swap2 交互：黑白黑三子、选白/交换/加两子、最终选色与玩家自动换边。
- 围棋分析新增黑白胜率、推荐着点和独立纯图片势力范围图；`/势力范围` 也走双方同意流程。
- 中国象棋、国际象棋及其他棋种统一展示双方胜率和推荐着法。
- 新增连珠禁手、Swap2 恢复、围棋势力图和插件同意流程回归测试。

## 2.0.0

- 将旧版单文件国际象棋插件重写为独立房间、规则引擎和渲染模块。
- 国际象棋支持 `/Nc3`、`/b1c3`、`/b1 c3` 及省略 `/` 的对应形式。
- 新增围棋（默认标准 19 路，可选 9/13 路）、中国象棋、五子棋、井字棋和黑白棋。
- 所有棋盘响应改为纯图片消息，并在棋盘内标记上一步起点和终点。
- 移除猫娘语气、头像网络请求及插件目录内的可变数据文件。
- 新增持久化恢复、并发锁、等待超时、管理员清理、统一战绩和棋谱。
- 新增双方逐次同意的本地轻量局势分析，不调用付费 LLM。
